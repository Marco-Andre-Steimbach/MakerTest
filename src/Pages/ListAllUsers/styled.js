import styled from "styled-components";

export const SectionListAll = styled.section`
    display: flex;
    flex-direction: column;
    background-color: var(--grey-color0);
    margin: 1rem 1rem;
    border: var(--primary-color-Focus) solid 2px;
    border-radius: 5rem;
    padding: 2rem;
`